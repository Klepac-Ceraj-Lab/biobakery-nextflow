#!/usr/bin/env nextflow
nextflow.enable.dsl=2

// 16S rRNA amplicon workflow — stub
// Planned: DADA2 / QIIME2 integration
workflow SIXTEENS {
    main:
    error "16S workflow is not yet implemented. Use --workflow mgx for shotgun metagenomics."
}
